name: stir-demo
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - stir>=6.3
  - numpy>=1.20
  - matplotlib>=3.3
  - scipy>=1.7
  - jupyter>=1.0
  - ipykernel>=6.0
  - pip
  - pip:
    - -r requirements.txt
